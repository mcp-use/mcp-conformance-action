name: Example - Single Server

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      
      # This is just an example - replace with your actual server setup
      - name: Setup server
        run: |
          echo "Setting up server..."
          # npm install
          # npm run build
      
      - name: Run conformance tests
        uses: ./
        with:
          mode: test
          servers: |
            [
              {
                "name": "example-server",
                "start-command": "echo 'Server would start here' && sleep 10",
                "url": "http://localhost:3000/mcp"
              }
            ]
